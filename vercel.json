{
  "version": 2,
  "framework": "vite",
  "buildCommand": "cd shipment-planner-ui && npm install && npm run build",
  "outputDirectory": "shipment-planner-ui/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
